import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  buttonContainer: {
    backgroundColor: '#eee',
    justifyContent: 'center',
    alignItems: 'center',
    width: '70%',
    alignSelf: 'center',
    padding: 10,
  },
});
