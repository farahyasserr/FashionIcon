import React from 'react';
import {ActivityIndicator} from 'react-native';

const Loader = (): JSX.Element => {
  //temporarily an activity indicator
  return <ActivityIndicator />;
};

export default Loader;
