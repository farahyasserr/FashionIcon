type ThemeColor = string;

type Colors = {
  black: ThemeColor;
  white: ThemeColor;
  darkGray: ThemeColor;
  lightGray: ThemeColor;
  price: ThemeColor;
};

const colors: Colors = {
  black: 'black',
  white: 'white',
  darkGray: 'gray',
  lightGray: '#eee',
  price: '#DD8560',
};

export default colors;
